# Execute v11 locally

Execute the registered division-reliability-v11 study from the currently checked-out branch `handover/division-reliability-v11-ready`.

This branch contains the complete v11 handover from commit `150aef2751b319ab3a4d1d877ae95cc543c2c56a`. No further branch switch, checkout, worktree creation, or file retrieval is required to read and execute the plan. References to `handover/division-reliability-v11` as a **Git branch** in the inherited documents identify the original planning branch: remain on `handover/division-reliability-v11-ready` and commit/push execution work here. References to `handover/division-reliability-v11/` as a **directory** are unchanged and valid. This routing clarification does not amend the frozen scientific study or its reference-file hashes.

Read `AGENTS.md`, then `handover/division-reliability-v11/CODEX_PROMPT.md` and every document it names. Follow that execution instruction with the branch-routing clarification above.

First reconcile and preserve existing local v10 work, processes, checkpoints, and reports. Reuse only provenance-qualified source-excluded upstream artifacts; otherwise implement and execute the inherited clean upstream prerequisite. Do not duplicate running training or modify historical studies.

Implement and evaluate C00, C01, and C11 in both embryo directions and both registered seeds. Follow the registered source-only sampling, calibration, resource limits, prediction-freeze protocol, official full-graph scoring, and cold image-to-CSV validation. The supplied planning contracts are not a trainer or a measured model result; proceed through actual local execution and report concrete blockers honestly.

Return `results/division-reliability-v11/REPORT_BACK.md` and `STATUS.json` with actual completion status, all required score comparisons, provenance, error attribution, resource measurements, and tested resume instructions. Commit and push implementation and sanitized results only on this ready branch. Keep raw data, weights, full predictions, private logs, and credentials outside Git. Preserve P0. Do not merge, submit to Kaggle, publish weights, or launch unrelated handover studies.
