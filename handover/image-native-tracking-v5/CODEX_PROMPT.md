# Execute the next iteration

Implement and execute `handover/image-native-tracking-v5/EXPERIMENTS.md` in this
repository with the user's selected GPT 6 Pro model and one RTX 4090. This is not
another planning request. The target is >=0.95 complete local score, compared with
C0/v3 A_residual_m3.0 at 0.934802374260586. Keep C0 as fallback.

Start by reading root AGENTS.md and relevant competition skills, then
`handover/multidata-training-v4/CONTINUATION.md`, the committed v4 report and
NEXT_AGENT, and every v5 specification. W400-W490 are complete: do not restart their
queues, historical seven-hour deadline, fits, dashboards or sealed outputs.

## Work to perform

Execute X500-X580. Build a true image-to-tracking comparison, not another small
fork-head experiment. Benchmark the official pretrained HOCT with properly
constructed object features; adapt its edge head on supported real transitions.
Independently fine-tune the installed native U-Net/transformer, first on fixed
candidates and then with controlled representation updates. Build real full-frame
new-peak/split hypotheses and let temporal evidence select them instead of merely
moving incumbent centers. Joint selection must include no-op, birth, continuation,
and bifurcation; represent missing context explicitly.

The existing data are sufficient for the primary work. Reuse the external synthetic
release only for selected controls or optional detector warm-up, not as another
primary pretraining sweep. No Zoo-to-Gaussian rendering extension is scheduled.
Public HOCT weights can be downloaded once, subject to the recorded checks and
source terms. FOCUS is optional only when an already authorized checkpoint can be
used locally. Do not accept gated contact-sharing conditions or upload microscopy
on the user's behalf. Lack of FOCUS access is not a study-wide blocker.

## Execution discipline

Preserve raw inputs, all v1-v4 locks/results, notebook originals, environments,
uncommitted user work and unrelated projects. Use explicit v5 roots; never mutate
v4 globals to redirect old stage commands. Resolve /root versus /home layouts from
actual files and overrides. The last v4 report had only 23.275 GiB free: check current
space, keep an 8 GiB reserve, stream patches/features, and store graph deltas rather
than duplicating every study. Do not delete user artifacts to make room.

Run real optimizer updates and inference. Log checkpoint keys/shapes, trainable
module names, gradient/parameter-change receipts, positive transitions and event
coverage, optimizer steps, CPU/GPU memory and wall time. Large step counts on cached
small features are not a substitute for native image-network training. Do not
silently replace HOCT with a new small MLP and keep its name.

Treat source train/dev results and pilot comparisons as operational exploratory.
Both embryos have been reused and upstream checkpoints/teachers may have seen
labels. Direct adaptation and calibration may use only the training-source embryo;
never calibrate on opposite-embryo labels, hidden test or a leaderboard. Freeze both
directions before each comparative full-data batch. Do not invent independent
confidence intervals from overlapping clips. No known selection rule can promise
hidden-test generalization here.

Unknown sparse annotations stay unknown. Preserve the v4 edge-mask correction:
one recorded child does not establish that a second daughter is absent. Train
supported incoming-parent choices; do not turn missing predecessors into known
births or weak quiet chains into certain nondivision labels. Keep all incidental
GT match/oracle attributes out of inference features.

The initial grid has at most 18 complete graph variants. Run the independent H and
N families and a new-observation pilot even if one family fails. Permit one bounded
interface/optimizer repair before declaring a family invalid. Source-only failure
diagnostics may route compute; no target-label hyperparameter loop. Carry out a
same-configuration second-seed replication for learned finalists. A sub-.95 gain
can be retained, but it is not target success. Never promote a pooled-only gain that
regresses an embryo under the registered gate.

## Deliver

Produce the complete 199-clip report/dashboard, per-sample official counts, model
and input manifests, source-only training receipts, headroom and coverage tables,
an image-to-submission inference package with C0 fallback, and a readable new
`CONTINUATION.md`. All learned proposal/gate/decoder components must execute during
fresh-image testing; reading the selected cached graphs is not sufficient.
If a family or a package test is blocked, preserve valid measurements and name the
missing experiment rather than declaring it done. Success may be a reproducible
negative result, but execution must go beyond scaffolding and another threshold sweep.

Commit and push only additive code, configs and sanitized results to this v5 branch
after reviewing the staged file list. Update v5 STATUS.json truthfully. Do not merge
PRs, submit to Kaggle, publish a notebook/forum post, rent hardware, or make paid API
calls. The user will review this iteration before selecting the next one.
