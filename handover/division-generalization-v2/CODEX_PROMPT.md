# Local Codex: implement and execute division generalization v2

The user wants >=0.95, not another tiny residual gain or a report consisting only
of audits. This branch preserves the actual completed previous study. Start from
P0 0.934864986413134, compare C4_m6 0.935178370257, and keep both unchanged.

Read root AGENTS.md and applicable competition skills, then EVIDENCE.md, PLAN.md,
SPEC.md, VALIDATION.md and study.json in this directory. The previous
pipeline-error-training handover is historical; its 48-hour matrix and automatic
178-update lock are NOT the execution instructions for this branch.

Implement tools/division_generalization_v2/ with isolated work and results roots.
Reuse verified bank, scorer, sparse-label, graph ownership, native-image and
fresh-inference utilities. Do not rewrite the whole tracker or repeat all D10,
D20, A10 and O10 families. Focus on representative negative event training,
complete-action utility, joint image evidence and adequate optimization.

Execute the cached-feature control, the new shared-scene division verifier with
random supported backgrounds, and its matched hard-negative-mining continuation.
The latter two share the same 2048-update prefix per direction/seed, then branch
for 2048 further joint updates. Checkpoint sharing must include optimizer/RNG
provenance and does not make them independent experiments. All main fits receive
4096 JOINT updates; optional identity pretraining does not count toward this floor.
Do not divide a large architecture list into the budget or silently shrink these
runs to pilots. A paused run remains incomplete, never a failed architecture.

Keep fitting/calibration/mining source-only in each direction. Unknown real
branches are NOT negatives. Learn from supported false forks and compare complete
edits, including lost links. Replace the previous unconditional native-logit
addition in the NEW module with learned relative action scores; native evidence
remains an input. Verify literal zero scores through the actual decoder, not an
inference bypass returning P0.

Make the image model cheaper per event by sharing one scene encoding across
candidate pairs, rather than encoding seven-frame crops repeatedly for each
node/alternative. Cache frozen representations only. Do not reuse trainable
encoder embeddings across optimizer updates. Measure loader/encoder/backward,
lease waits, checkpoint overhead and inference separately.

Use one RTX 4090, 24 GB VRAM, 16 cores, 64 GB RAM; cap total GPU use at 20 GiB and
study process-tree RSS at 50 GiB. Inspect actual running processes and coordinate
existing leases. Do not kill/resume other jobs, delete old artifacts, install into
shared environments, or interpret stale progress files as live jobs.

Source-only qualification controls target evaluation, not whether the main model
gets trained. Both main arms must receive their adequate training attempt even if
the cheap control fails. Fix implementation defects without changing scientific
settings based on target results. Preserve invalid attempts and exact reasons.

Deliver actual code, tests, source training curves, sampling/exposure manifests,
full source screens, nominated target predictions on all 199 clips, both embryos,
replications, error transitions, runtime and fresh-image proof. Write concise
results/division-generalization-v2/REPORT.md and CONTINUATION.md. State >=0.95
achieved only for measured full graphs, with the validation scope attached.
No default promotion, merge, Kaggle submission, weight publication or LB claim.
Commit code/results on this new branch; preserve unrelated local work.
