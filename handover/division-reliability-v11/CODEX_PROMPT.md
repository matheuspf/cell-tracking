# Local execution instruction

Execute v11 on `handover/division-reliability-v11`. Read AGENTS.md, the root DIVISION_RELIABILITY_V11_START_HERE.md, REVIEW.md, PLAN.md, IMPLEMENTATION.md, VALIDATION_REPORTING.md and study.json in this directory. Those files define one study; do not launch v10/v9 or historical experiment queues as additional campaigns.

The reviewer found no remotely published v10 implementation or result report. First reconcile the actual local state and preserve it. This is not permission to assume the v10 experiment failed or to duplicate active training. Reuse only source-excluded C00 artifacts with qualified provenance and identical recipe. Copy resumable state into v11-owned storage; preserve old files. Otherwise implement the inherited upstream prerequisite with random initialization of every component.

Build and run C00, the CPU learned fork comparator C01, and the compact factorized model C11 in both source directions and both seeds. Use one prediction-only action bank, sparse-safe labels, source-only hard-negative mining, source-only calibration and exact graph/CSV scoring. Full clean validation is the main task; an exposed P0 improvement cannot replace it.

Implement the described stage CLI, real source tests, training, inference and report. The supplied standard-library contracts are reference checks only, not a complete trainer, security sandbox, official scorer or tested GPU implementation. Do not stop after passing them.

Inspect real metadata, dependencies, GPU, memory, disk and active processes. Check competition references using maintained skills/extractor with normal throttles. Do not accept new terms, change accounts or introduce paid services. Missing current rules block dependent submission/external actions, not lawful source-only development with already accessible inputs. Record any verification limitation.

Freeze recipes and budgets before production runs; freeze every retained model and prediction before target score access. Record any historical/local target inspection honestly. Scientific amendments must be explicit and symmetric before new target outcomes; afterward retain the registered result and propose the next study rather than tuning it.

Keep source/calibration/target process boundaries, semantic dependency manifests and pre-import read guards. Masks for unknown labels never become biological negatives. Do not use GT node counts, annotation density or matched IDs as model inputs. Inference takes an explicit package and images, not a source inferred from unfamiliar dataset names.

Preserve P0, old reports, checkpoints, unfinished jobs, raw data and user changes. Recover from an implementation failure with a named receipt and identical scientific recipe when possible. Do not silently replace a failed direction with C00/P0 and count it as C11. A deliberately disabled source-safety policy is allowed only when visibly labeled as such.

At completion commit and push the implementation, configurations, tests and sanitized evidence to this branch only. Keep weights, arrays, complete graphs, raw microscopy, personal paths/logs and credentials out of Git. No merge, Kaggle submission, weight publication or production replacement is authorized.

Return `results/division-reliability-v11/REPORT_BACK.md` and a compact `STATUS.json` with complete/partial/blocked statuses, exact artifact paths/hashes and tested resume commands. Report actual scores, no-op status, all four cells, source/target exposure limits, errors, compute and the next decision. Do not promise an unseen 0.95 score or describe a planning/test result as a trained model.
